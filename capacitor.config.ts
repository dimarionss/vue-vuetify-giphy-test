import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'vue.test.vuetify.project.giphy',
  appName: 'vuetify-project',
  webDir: 'dist'
};

export default config;
